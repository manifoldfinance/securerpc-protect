# Installing SecureRPC

This guide will walk you through the process of installing SecureRPC in your project.

## Prerequisites

Before you begin, ensure you have the following installed:
- Node.js (version 14 or later)
- npm or yarn

## Installation Steps

1. Create a new directory for your project:
   ```bash
   mkdir my-SecureRPC-project
   cd my-SecureRPC-project

